package dynamicProgramming;


public class PalindromePartitionII {
    public int minCut(String s) {
        int len = s.length();
        boolean[][] isPandrome = new boolean[len][len];
        for(int i = 0; i < len; i++){
            for(int j = 0; j < len; j++){
                
            }
        }
    }
}
